superupdate() {
  sudo apt-get update && \
    sudo apt-get -y --force-yes upgrade && \
    sudo apt-get -y dist-upgrade && \
    sudo apt-get -y autoremove && \
    sudo apt-get -y clean
}

export VIMRUNTIME=~/contrib/vim/runtime/
