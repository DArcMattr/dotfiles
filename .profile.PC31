#!/usr/bin/env zsh

export CC=clang
export CXX=clang++
export DISPLAY=:0.0

# Adding wslview via xdg-open as a browser for WSL
if [[ $(uname -r) == *Microsoft  ]]; then
  if [[ -z xdg-open  ]]; then
    export BROWSER=wslview
  else
    export BROWSER=xdg-open:wslview
  fi
fi

xrdb ~/.Xresources
