# XXX pull back into .aliases under a feature check
superupdate() {
  \sudo \sh -c "\portsnap fetch update && \
    \sudo \portmaster -aD && \
    \sudo \pkg2ng && \
    \sudo \freebsd-update fetch &&
    \sudo \freebsd-update install"
}
